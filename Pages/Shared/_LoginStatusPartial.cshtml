@if (User.Identity.IsAuthenticated)
{
	<form method="post" class="form-inline" asp-page="/Account/Logout">
		<button type="submit" class="ml-2 btn btn-danger">Logout</button>
	</form>
}
else
{
	<a class="btn btn-info" asp-page="/Account/Login">Login</a>
}